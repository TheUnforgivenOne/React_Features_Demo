import styled from '@emotion/styled';

export const ModalWrapper = styled.div`
  background: #FFF;
  position: absolute;
  min-width: 400px;
  height: auto;
  margin-top: 30px;
  margin-left: calc(50% - 200px);
  padding: 10px;
`;
